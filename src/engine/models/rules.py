# from django.db import models
#
#
# class Rule(models.Model):
#     """
#     The Rule - is the model containing all necessary information needed for creating domains.
#     """
#
#     name = models.CharField(unique=True, help_text="name of the Rule")
#
#     domain_zone = models.ForeignKey
#
#     state = models.JSONField(null=True, blank=True, help_text="needed for saving state data")
#